import CommonAdapter from '../../plugin_common/Adapter/CommonAdapter';

let urls = {};
/**
 * saveAppointment  保存定时任务
 * getAppointment   获取定时任务信息
 */

// urls.saveAppointment = 'https://ms.viomi.com.cn/home/airConditioner/timing?token=' + CommonAdapter.viomiToken;
// urls.getAppointment = 'https://ms.viomi.com.cn/home/airConditioner/timing?token=' + CommonAdapter.viomiToken + '&did=' + CommonAdapter.deviceId;

export default urls;
